services:
  odoo:
    restart: always
    image: TODO # repository url
    environment:
      - MARABUNTA_MODE=prod
      - SENTRY=True
      - SENTRY_DSN=TODO
      - PGSSLMODE=require
      - PGHOST=TODO
      - PGDATABASE=TODO
      - PGUSER=TODO
      - LIMIT_TIME_CPU=TODO
      - LIMIT_TIME_REAL=TODO
      - ODOO_BASE_URL=https://TODO
    volumes:
      - ~/data/${COMPOSE_PROJECT_NAME}/addons:/data/odoo/addons
      - ~/data/${COMPOSE_PROJECT_NAME}/filestore:/data/odoo/filestore
      - ~/data/${COMPOSE_PROJECT_NAME}/sessions:/data/odoo/sessions
    ports:
      - 8069:8069
      - 8072:8072
version: '3.7'
