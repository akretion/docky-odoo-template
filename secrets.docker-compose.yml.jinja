# never commit this file
version: "3.7"
services:
{% if env == "dev" %}
  odoo:
    environment:
      - PGHOST={{PGHOST}}
      - PGDATABASE={{PGDATABASE}}
      - PGPASSWORD={{PGPASSWORD}}
      - PGUSER={{PGUSER}}
{% endif %}